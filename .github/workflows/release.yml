name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  # Build dashboard assets first (shared by all platform builds)
  build-dashboard:
    name: Build Dashboard
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: cd apps/dashboard && npm install

      - name: Build dashboard
        run: cd apps/dashboard && npm run build

      - name: Package dashboard assets
        run: |
          mkdir -p dashboard-dist
          if [ -d "apps/dashboard/dist/dashboard/browser" ]; then
            cp -r apps/dashboard/dist/dashboard/browser/* dashboard-dist/
          elif [ -d "apps/dashboard/dist/dashboard" ]; then
            cp -r apps/dashboard/dist/dashboard/* dashboard-dist/
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: dashboard-assets
          path: dashboard-dist/

  # Build VS Code extension
  build-vscode:
    name: Build VS Code Extension
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: cd apps/vscode && npm install

      - name: Compile extension
        run: cd apps/vscode && npm run compile

      - name: Package extension
        run: |
          cd apps/vscode
          npx vsce package --no-dependencies
          mv *.vsix ../..

      - uses: actions/upload-artifact@v4
        with:
          name: vscode-extension
          path: "*.vsix"

  # Build Go binaries for all platforms (requires native runners for CGO)
  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    needs: build-dashboard
    strategy:
      matrix:
        include:
          - os: macos-latest
            goos: darwin
            goarch: arm64
            name: palace-darwin-arm64
          - os: macos-latest
            goos: darwin
            goarch: amd64
            name: palace-darwin-amd64
          - os: ubuntu-latest
            goos: linux
            goarch: amd64
            name: palace-linux-amd64
          - os: ubuntu-latest
            goos: linux
            goarch: arm64
            name: palace-linux-arm64
          - os: ubuntu-latest
            goos: windows
            goarch: amd64
            name: palace-windows-amd64.exe
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Download dashboard assets
        uses: actions/download-artifact@v4
        with:
          name: dashboard-assets
          path: internal/dashboard/dist

      - name: Verify dashboard assets
        run: ls -la internal/dashboard/dist/

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          COMMIT=$(git rev-parse --short HEAD)
          DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          go build -ldflags="-s -w -X main.version=$VERSION -X main.commit=$COMMIT -X main.date=$DATE" -o ${{ matrix.name }} ./cmd/palace

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: ${{ matrix.name }}

  # Create GitHub release
  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [build, build-vscode]
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Prepare release files
        run: |
          mkdir -p release

          # Copy binaries
          for dir in dist/palace-*/; do
            cp "$dir"* release/ 2>/dev/null || true
          done

          # Copy VS Code extension
          cp dist/vscode-extension/*.vsix release/ 2>/dev/null || true

          ls -la release/

      - name: Generate checksums
        run: |
          cd release
          sha256sum * > SHA256SUMS
          cat SHA256SUMS

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            release/*
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref, 'rc') || contains(github.ref, 'beta') || contains(github.ref, 'alpha') }}
